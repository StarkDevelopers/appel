{
  "name": "appel-backend",
  "version": "1.0.0",
  "scripts": {
    "dev": "nodemon server.js",
    "frontend": "cd ../frontend && npm run build && cd build && copyfiles \"./**/*\" ../../backend/client && cd ../../backend",
    "start": "npm run frontend && node server.js",
    "lint": "eslint ."
  },
  "dependencies": {
    "copyfiles": "^2.2.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "eslint": "^6.8.0",
    "eslint-config-prettier": "^6.11.0",
    "eslint-plugin-prettier": "^3.1.3",
    "express": "^4.17.1",
    "multer": "^1.4.2",
    "nodemon": "^2.0.3",
    "socket.io": "^2.3.0"
  },
  "devDependencies": {
    "prettier": "^2.0.5"
  }
}
